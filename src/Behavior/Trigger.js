const ConfigurableBehavior = require('./ConfigurableBehavior');

/**
 * A Trigger definition:
 * Defines a conditional event that can be observed by Timbot that leads to one or more action(s) being executed.
 */
class Trigger extends ConfigurableBehavior {

}

module.exports = Trigger;
